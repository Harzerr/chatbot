name: Repository hygiene

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate-tracked-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Reject runtime data and oversized files
        run: scripts/check_repository_hygiene.sh
